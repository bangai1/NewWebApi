@* @model IEnumerable<ConsumeWebAPI.Models.BillsViewModel>

@{
    ViewData["Title"] = "Search Bills";
}

<div class="container mt-5">
    <h2 class="text-center mb-4">Search Customer</h2>
    <form asp-action="SearchBills" asp-controller="Bills" method="get" class="form-inline justify-content-center mb-4">
        <div class="form-group mx-sm-3 mb-2">
            <label for="cust_id" class="sr-only">Customer No:</label>
            <input type="text" name="cust_no" id="cust_no" class="form-control" placeholder="Enter Customer No" />
        </div>
        <button type="submit" class="btn btn-primary mb-2">Search</button>
    </form>

    @if (!string.IsNullOrEmpty(ViewBag.ErrorMessage))
    {
        <div class="alert alert-danger text-center">@ViewBag.ErrorMessage</div>
    }

    @if (Model != null && Model.Any())
    {
        <table class="table table-striped table-bordered">
            <thead class="thead-dark">
                <tr>
                    <th>Bill ID</th>
                    <th>Customer Number</th>
                    <th>Company Number</th>
                    <th>Amount</th>
                    
                </tr>
            </thead>
            <tbody>
                @foreach (var bill in Model)
                {
                    <tr>
                        <td>@Html.DisplayFor(modelItem => bill.ID)</td>
                        <td>@Html.DisplayFor(modelItem => bill.Cust_No)</td>
                        <td>@Html.DisplayFor(modelItem => bill.Company_No)</td>
                        <td>@Html.DisplayFor(modelItem => bill.Status)</td>
                        
                    </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <div class="alert alert-info text-center">No bills found</div>
    }
</div>

 *@

@model IEnumerable<ConsumeWebAPI.Models.BillsViewModel>

@{
    ViewData["Title"] = "Search Bills";
}

<div class="container mt-5">
    <h2 class="text-center mb-4">Search Customer</h2>
    @* <form asp-action="Index" asp-controller="Bill_Search_Controller" method="get" class="form-inline justify-content-center mb-4"> *@
        <div class="form-group mx-sm-3 mb-2">
            <label for="cust_no" class="sr-only">Customer No:</label>
            <input type="text" name="cust_no" id="cust_no" class="form-control" placeholder="Enter Customer No" required />
        </div>
        <button type="button" class="btn btn-primary mb-2" id="submitBtn">Search</button>
    @* </form> *@

 @section Scripts {
    <script>
        $("#submitBtn").on("click", function () {
            console.log("test")
        })
    </script>
 }

    @if (!string.IsNullOrEmpty(ViewBag.ErrorMessage))
    {
        <div class="alert alert-danger text-center">@ViewBag.ErrorMessage</div>
    }

    @if (Model != null && Model.Any())
    {
        <table class="table table-striped table-bordered">
            <thead class="thead-dark">
                <tr>
                    <th>Bill ID</th>
                    <th>Customer Number</th>
                    <th>Company Number</th>
                    <th>Amount</th>
                    <th>Date</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var bill in Model)
                {
                    <tr>
                        <td>@Html.DisplayFor(modelItem => bill.ID)</td>
                        <td>@Html.DisplayFor(modelItem => bill.Cust_No)</td>
                        <td>@Html.DisplayFor(modelItem => bill.Company_No)</td>
                        <td>@Html.DisplayFor(modelItem => bill.Status)</td>
                        <td>@Html.DisplayFor(modelItem => bill.Subs_No)</td>
                    </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <div class="alert alert-info text-center">No bills found</div>
    }
</div>
