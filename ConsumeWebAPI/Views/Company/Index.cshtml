@model IEnumerable<ConsumeWebAPI.Models.CompanyViewModel>

 @*    @{
        ViewData["Title"] = "Company List";
    }

    <h2>Company List</h2>

   

    <div class="container mt-4">
        <form asp-controller="Company" asp-action="Index" method="get" class="form-inline mb-3">
            <div class="form-group mr-2">
                <input type="text" name="searchString" value="@ViewData["CurrentFilter"]" class="form-control" placeholder="Search Company..." />
            </div>
            <button type="submit" class="btn btn-primary">Search</button>
        </form>

        @if (Model != null && Model.Any())
        {
            <table class="table table-striped table-bordered">
                <thead class="thead-dark">
                    <tr>
                        <th>Company Name</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var company in Model)
                    {
                        <tr>
                            <td>@company.Company_Name</td>
                        </tr>
                    }
                </tbody>
            </table>
        }
        else
        {
            <div class="alert alert-info" role="alert">
                No companies found.
            </div>
        }
    </div>

 *@


<!DOCTYPE html>
<html lang="en">

<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-1.0, initial-scale=1.0">
	<link rel="stylesheet" type="text/css"  href="style.css">
	<title>Custom Select Menu with Search Box</title>
</head>

<body>
    <div class="search_select_box">
        <select>
            <ul>
                @foreach (var company in Model)
                {

                    <li>@company.Company_Name</li>

                }
            </ul>
        </select>

    </div>






</body>






</html>
